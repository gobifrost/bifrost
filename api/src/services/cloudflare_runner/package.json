{
  "name": "@gobifrost/cloudflare-builder-runner",
  "private": true,
  "scripts": {
    "build": "esbuild worker.mjs --bundle --format=esm --platform=node --external:cloudflare:* --outfile=dist/worker.js",
    "test": "node --test runtime_helpers.test.mjs"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.5",
    "wrangler": "4.120.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
