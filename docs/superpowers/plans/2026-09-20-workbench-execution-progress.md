# Workbench execution progress

Owner consumes executor completion and advances the authorized plan. No raw executor-completion notifications to the user. No Bifrost commits/pushes/merges performed in this execution.

- Skill: generic `handoff` created, validated, installed in Codex/Claude, published on personal-skills branch `feature/generic-handoff`. Latest correction keeps executor completion/internal continuation with owner.
- Phase 0: Muse mockup returned. Primary reran browser checks, inspected rendered states, fixed generation wiping inputs, Cancel mutation leakage, stale profile comparisons, selection prefill and results visibility. Extended browser tests passed. This is a design prototype, not shipped product.
- Phase 1: Muse contract inventory returned. Primary rejected proposed fail/insufficient precedence, blanket simulator-state N/A and conflated job/verdict success. Approved semantics are in `2026-09-20-recorded-core-approved-handoff.md`.
- A1 backend core: Muse session `ses_f407361e0ffeZ3c5QxsnLq2I87`, log `/tmp/workbench-recorded-core-muse.jsonl`. After excessive read-only reconnaissance, owner stopped the process and resumed the SAME session/model with a directive to implement the approved files; resolved import-layer uncertainty without refactoring. Resume process `/tmp/resume-workbench-recorded-core.py`, exec handle `24864`. Await code/test report, then independently review and verify.
- A2 evidence projection: exact next packet prepared in `2026-09-20-recorded-evidence-approved-handoff.md`; execute after A1 accepted.
- Remaining: A2, durable admission/API/CLI including applicability/judge decisions, reviews and scheduling, generated scenarios/proposed tools, production UI integration, focused/full gates and intent review. These are not complete or silently dropped.

Primary must remain active while executor runs or use a supported owner-resumption mechanism. A process finishing is not acceptance. Do not require the user to relay notifications.

## A1 accepted / A2 launched

A1 primary review fixed four correctness/redaction issues and corrected two bad test expectations. Independent focused run: 64 passed; API quality: pyright 0 errors/warnings and Ruff passed. See recorded-core-report.md. Pure core only, no API/CLI claims.

A2 launched using SAME Muse session `ses_f407361e0ffeZ3c5QxsnLq2I87` and its accumulated repository context. Log `/tmp/workbench-recorded-evidence-muse.jsonl`, launcher `/tmp/launch-workbench-recorded-evidence.py`, exec handle `12776`. Allowed files are defined in recorded-evidence-approved-handoff.md. Primary remains active to consume return; no user completion ping.

## A2 provider interruption

A2 executor returned exit 1 after upstream Muse HTTP 503 `service_overloaded` (2026-09-20 16:49:57 UTC), before writing its allowed source/test files. No implementation claimed. One bounded retry launched on SAME model/session, reusing inspected source context and directing implementation rather than repeated reconnaissance. Launcher `/tmp/retry-workbench-recorded-evidence.py`, exec handle `35960`, same append-only log. Do not silently switch model or retry indefinitely if this fails again. A1 remains accepted with 64 tests + API quality green.

## A2 first source review

Retry produced `api/shared/agent_recorded_evidence.py`. Primary found absent completion-journal proof, unbounded/hidden descendant traversal, weak journal/ledger identity checks, redaction-before-completeness ordering, partial usage labeled complete, and cycle risk. Stopped executor PID 1308927 cleanly before primary feedback, then resumed SAME Muse session/model with `2026-09-20-recorded-evidence-review.md`. New launcher `/tmp/resume-workbench-evidence-review.py`, exec handle `21297`; same append-only log. This is a code-review continuation, not an additional provider-failure retry. No A2 acceptance or test claim yet. Primary has not edited executor-owned source.

## Owner stopped stalled A2 attempt (2026-09-20 17:27 UTC)

After returning the revised source and unit-test file, Muse produced no new log event for over five minutes (last event timestamp 1789924863232). Owner stopped only PID 1526562; exec handle 21297 terminated. This is an observed output stall, not a confirmed second HTTP error. No executor remains owned/running for this packet. Preserve session `ses_f407361e0ffeZ3c5QxsnLq2I87` and append-only log for explicit resumption.

A2 NOT ACCEPTED: e2e file/report and Docker checks outstanding. Primary identified remaining concerns in revised source: usage-row coverage is still not established from intact journal alone, and test `_add_run` returns an ORM object where many callers use it as UUID. These need correction before running/accepting tests. No Bifrost commit/push; no raw completion notification. A1 remains last verified milestone (64 focused tests + API quality). Full API/CLI/UI work remains in master plan.

## Native executor authorized (2026-09-20)

User authorized Terra/Luna or another suitable native subagent. Initial default-role spawn with Terra selector incorrectly resolved to unsupported gpt-5.4-mini and failed before work. Retried using implementation-worker role with Terra selector: `/root/evidence_worker` is active; actual model identity is harness-dependent (worker role documents gpt-5.5). Primary retains acceptance. Worker owns only original A2 files and report; OpenCode remains stopped. Native completion returns internally to owner. Do not launch another writer for A2.

## A2 accepted; native B1 active

Native worker `/root/evidence_worker` completed A2. Primary reviewed and independently ran combined A2/A1/assertions: 103 passed. Final review corrected authorized-only descendant bound, null-safe hidden-child existence and negative iteration counters. Worker final A2: 42 passed + API quality green. Primary independently ran the final 3 regressions: 3 passed (37 deselected). Logs `/tmp/workbench-native-a2-review-tests.log` and `/tmp/workbench-native-a2-final-review.log`. No known A2 failures; observational token/cost totals intentionally cannot prove complete historical budgets.

B1 API/CLI now handed to SAME native worker using `2026-09-20-recorded-api-approved-handoff.md`, including primary-reviewed dedupe/fencing/privacy/CLI clarifications. Worker sole writer of B1 ownership; A1/A2 excluded. Native task completion returns internally; primary remains owner. No Bifrost commits/push or notification. Full semantic/reviews/schedules/designer/production UI phases remain unfinished.

## B1 primary review and verification in progress

Native worker returned B1: 189 focused regression/DTO tests, 2 HTTP real-scheduler tests, API quality, generated types and client tsc reported passing. Primary independently ran admission, CLI, contract version and HTTP recorded tests: 25 passed, 2 deprecation warnings (`HTTP_422_UNPROCESSABLE_ENTITY`). Log `/tmp/workbench-b1-primary-tests.log`.

Primary has NOT accepted B1 yet: worker completing two final reviewed corrections (missing frozen source references must fail closed; lease expiry rechecked after acquiring a possibly delayed DB lock), plus current422 constant spelling. Worker owns B1 files until return; primary test run97507 finished, stack free. Another read-only worker `/root/recorded_api_plan` is inventorying semantic judge B2; no B2 writes authorized yet.

OpenCode session ses_f407361e0ffeZ3c5QxsnLq2I87 remains stopped. Primary verified `/proc` had no exact-session OpenCode process when user asked; last log timestamp17:21:03UTC. Native agents are the only task executors.

## B1 accepted; usage-accounting requirement added

Primary reviewed the final fail-closed source authorization, batched frozen-source authorization, post-lock database-time lease check, and populate_existing refresh. Independently ran `./test.sh tests/unit/services/agent_evaluations/test_recorded_admission.py -v`: 12 passed, no failures/skips, confirmed in per-worktree JUnit. Log `/tmp/workbench-b1-final-primary.log`. Worker also reported final API quality green; its quality log confirms 0 errors/warnings and Ruff pass. Earlier independent B1 HTTP/CLI/contract verification: 25 passed before the final fixes. B1 exact recorded evaluation is accepted. Full backend/UI suites are still pending integrated implementation.

User explicitly requires testing/review accounting like agents, including per-run and global provider/MSP breakdowns. Updated master acceptance criteria. B2's earlier proposal to retain judge usage only in result JSON and defer global AIUsage integration is superseded; packet is marked pending accounting revision and must not be executed as written. Read-only native inventory confirmed existing synthetic semantic judges also omit shared AIUsage records. `/root/recorded_api_plan` is drafting a bounded accounting contract; `/root/evidence_worker` is inspecting synthetic call identities, failure accounting, and transaction boundaries. No source writers or OpenCode executor active during this design step.

## Shared accounting foundation handed off

Primary approved `2026-09-20-quality-usage-foundation-approved.md`: extend AIUsage with operation provenance and use shared call-attempt records solely for accounting uncertainty, never as a job lifecycle. Separate factual spend recording from verdict fencing so cancellation cannot erase observed spend. No fake source-run attachment, duplicated synthetic rows, missing-cost zeros, raw config storage, or network pricing under DB locks. `/root/recorded_api_plan` owns only foundation schema/service/tests; it must wait for stack release before testing. `/root/evidence_worker` owns only the existing synthetic-judge malformed-response usage-loss fix and focused tests. Their files are disjoint. Reporting, caller integration, UI/CLI usage presentation, and B2 judging remain pending subsequent review. Primary remains active for both completions.

Synthetic-judge usage preservation accepted: worker's 22 assertion tests and API quality passed. Primary reviewed the diff and independently ran `./test.sh tests/unit/services/agent_evaluations/test_assertions.py -k 'preserves_usage or does_not_invent_usage' -v`: 3 passed, 19 deselected. Malformed JSON and out-of-range verdict retain observed tokens/cache/provider cost; no-response errors retain empty usage. Log `/tmp/workbench-judge-usage-primary.log`. This fixes result provenance only; no global accounting integration claim. Test stack released to foundation worker.

## Quality accounting foundation accepted

Native worker completed the migration/schema/internal shared accounting service. Primary review corrected ORM refresh under locks, database cost precision idempotency, required category/reason metadata, test-owned committed-row cleanup, meaningful legacy-context constraint tests, and attempt-time reporting timestamps. Removed an unused speculative public attempt DTO (no public API exists in this slice).

Worker: 17 focused tests, 1 database integration test, 42 legacy service tests, API quality all passed; migration head `20260920_quality_usage`. Primary independently ran `./test.sh tests/unit/services/test_quality_usage.py tests/e2e/api/test_quality_usage_accounting.py tests/unit/services/test_ai_usage_service.py tests/unit/services/test_ai_usage_agent_run.py -v`: 60 passed, no errors/failures/skips, JUnit confirmed. Log `/tmp/workbench-quality-usage-primary.log`. Broader backend/client suites not run for this foundation slice. B2 packet now approved against accepted shared accounting. Global reports, UI/CLI breakdowns and existing synthetic-judge writer integration remain pending; no overall feature-completion claim.

B2 `/root/evidence_worker` owns recorded semantic admission/judge/CLI/contracts/tests per approved semantic packet. R1 `/root/recorded_api_plan` owns only new shared reporting core and its tests per reporting-core-approved packet, with no DTO/routes/UI ownership. B2 has test-stack priority; R1 must wait for release. Primary early reviews identified per-assertion resume (not any-completed pair shortcut), strict structured model output, canonical argument projection, no invented profile names, and SQL-bounded reporting groups instead of loading all operation combinations. These are active corrections, not verified milestones. Native worker returns remain internal to primary; no raw user completion notification.

R1 SQL aggregation source completed; primary corrected unbounded combined-group loading, UNION column alignment, safe JSON boolean comparison, and explicit scope validation. Worker and primary independently ran `./test.sh tests/unit/services/test_quality_usage_reporting.py -v`: 5 passed each. Primary log `/tmp/workbench-quality-reporting-primary.log`. Shared API quality initially caught two RowMapping annotation errors in R1; owner corrected them. Final combined quality still pending. R1 core is focused-test verified, not yet exposed by API/CLI/UI.

B2 initial source/helper verification returned, but primary withheld acceptance for missing real persistence tests. Expanded handler tests exposed invalid requester fixtures and leaked committed test data (four failures in a 60-test combined run). Primary confirmed db_session only rolls back current transactions; committed B1/B2 fixtures require explicit owned-row cleanup. Worker is correcting those causes, testing actual accounting after cancellation/lease loss, frozen assertion identity on resume, terminal ambiguity projection, semantic CLI flag/policy, and OpenRouter accounting identity. No failure is waived. B2 currently owns the test stack. R1 writer is idle after reporting/auth inventory. Shared foundation itself remains independently verified (60 tests); no overall completion claim.

## B2 independent verification and reporting exposure

Primary independently ran the combined semantic/admission/assertion/recorded CLI/contract/HTTP/reporting command: 69 passed, zero errors/failures/skips, confirmed in per-worktree JUnit. Log `/tmp/workbench-semantic-reporting-primary.log`. API quality passed with 0 errors/warnings and Ruff clean; log `/tmp/workbench-semantic-reporting-quality.log`. Primary then corrected a reporting coverage inconsistency: grouped missing-cost coverage stayed zero despite nonzero missing-cost totals. Added the corresponding assertion; focused recheck is running in `/tmp/workbench-reporting-coverage-primary.log`.

Public reporting packet `2026-09-20-quality-usage-public-reporting.md` is assigned to evidence_worker: global existing Usage reports subroute and authorized recorded-operation reporting, CLI parity, typed DTOs. Synthetic execution reads/results/cancel and batches had only tenant checks; same-tenant private-agent access and null-org access require correction before per-operation reporting. `recorded_api_plan` owns that correction under `2026-09-20-quality-execution-authorization-handoff.md`. Missing/deleted baselines fail closed for non-admins; no tenant-only historical fallback. Primary owns integration and reviews.

Blocking accounting gaps identified (not waived): synthetic semantic judges only store JSON usage and call providers while holding result transactions; native synthetic_accounting_plan is producing the smallest correction proposal. Legacy tuning consolidated proposal does not record usage, tuning chat/dry-run coerce missing tokens to zero, and summarizer only records successful parsed calls. Tuning replacement must remove these old paths or account for them before completion; summarizer failure accounting needs a focused correction. Designer/simulation runtime usage already exists in AIUsage and must not be duplicated; legacy coverage is explicitly unknown. No claim that all overhead is accounted yet. Production UI, full gates, and broader workbench implementation remain unfinished.

## Authorization correction accepted; accounting integrations active

Primary independently ran `./test.sh tests/e2e/api/test_agent_evaluation_authorization.py tests/e2e/api/test_agent_evaluation_api.py -v`: 14 passed; `/tmp/workbench-execution-auth-primary.log`. Review added actual null-org-principal and missing-matrix-member regressions, independently run with `./test.sh tests/e2e/api/test_agent_evaluation_authorization.py -k 'null_org_principal or batch_missing_member' -v`: 2 passed, 7 deselected; `/tmp/workbench-execution-auth-edge-primary.log`. This closes the bounded synthetic detail/results/cancel/batch access gap. Later integrated quality still required after concurrent edits.

Final R1 grouped missing-cost coverage regression passed: 5 tests, `/tmp/workbench-reporting-coverage-primary.log`. Public reporting/CLI worker owns the test stack next. Summary usage source/tests are implemented but await verification; preserves billed malformed/empty responses and per-call retry cost, rejects missing/invalid token counts instead of inventing zero. Known legacy no-response/partial-token accounting gaps remain documented; no completeness claim.

Synthetic accounting proposal was revised and approved only after primary identified the live-call versus lost-call race. It now uses canonical child PlatformJobs and durable result linkage, not custom leases or blind ambiguity based on attempt age. It preserves non-authoritative synthetic semantics. A proposed mixed-definition alignment defect was disproved: `_score_result` already stores only semantic definitions; no speculative bug fix is authorized. Worker owns implementation in shared business logic and existing job coordination, with primary review still required.

## Usage reporting and synthetic completion review

Global and recorded-evaluation usage API/CLI implemented. Reports expose Decimal costs, tokens/cache counts, purpose/provider/profile/organization groups, missing-cost coverage and unresolved accounting attempts. Existing Usage reports remain compatible. New multi-query report routes use an independent repeatable-read snapshot; a concurrent-commit regression proves overall totals and groups agree. Read authorization is applied before recorded operation reporting. Production usage UI remains unfinished.

Primary combined semantic/admission/summary/reporting/CLI/contract/HTTP verification: 83 passed, one fixture FK failure. Fixed the fixture's nonexistent organization reference and then missing required PlatformJob name; final targeted HTTP authorization and negative-token tests passed (2). Logs `/tmp/workbench-usage-public-summary-primary.log` and `/tmp/workbench-usage-http-fixture-primary.log`. Summary accounting now retains observed billed malformed/empty responses and separate paid retries; legacy missing-response/partial-token limitations remain explicit.

Primary independently ran synthetic helpers/jobs/completion races/runtime completion/reporting/HTTP/contract/import hygiene: 80 passed, log `/tmp/workbench-usage-synthetic-snapshot-primary.log`. Subsequent source review required strict persisted child linkage, actual child payload checks, paid marker identity checks, and deferring parent finalization until semantic children settle. Latest focused run of job module, completion race module, and reporting core: 43 passed, one newly edited test used unsupported generic-JSON `.astext`; worker owns correction to `.as_string()` and focused verification. Log `/tmp/workbench-synthetic-final-flow-primary.log`. Full child execution → parent completion flow passed. Snapshot test now scopes its data to a unique organization. No acceptance of the newest synthetic patch until remaining correction and scheduler dispatch review finish.

OpenCode remains stopped. Native synthetic worker owns test stack after primary run completed; evidence worker is drafting the smallest authorized per-synthetic-execution/designer usage read contract. No broader backend/UI completion claim, commit, push, or completion notification.

## Synthetic accounting source accepted; operation reporting final review

Worker corrected the generic JSON test comparator, proved scheduler dispatch continues while a semantic child is active, and ran the two affected regressions plus reporting core: 12 passed. API quality: zero Pyright errors/warnings and Ruff passed. Primary reviewed the full completion and scheduling paths; synthetic accounting source is accepted as this bounded backend slice. Primary also aligned SQL UNION monetary/count column positions (the attempt-side zero values had masked the mismatch). Follow-up test fixture cleanup now tracks recreated child jobs and source runs using primitive IDs across rollback; its latest cleanup-only changes await the next primary combined run.

Per-synthetic-execution and root Test Designer usage API/CLI are implemented by evidence_worker, under final review. Reports derive operation IDs from authorized domain records, preserve original-agent access, enforce root identity and exact tenant match, and flag legacy runtime coverage unknown even for zero observed rows. Primary found the shared human CLI formatter expected flat rows while real DTOs have nested totals; worker corrected it and changed fixtures to validate against the response model. Latest observed worker JUnit: 21 passed. Final contributor report, independent combined accounting verification, and production UI integration remain pending.

Phase 3A review data foundation is delegated separately to recorded_api_plan under the revised proposal. Approved ownership is only ORM, additive migration, findings provenance fields and focused data tests; no runtime/API/CLI/UI implementation is approved by that handoff. Canonical PlatformJob retains lifecycle, review admission retains immutable provenance, and billing continues to use the shared ledger. Testing stack remains with evidence_worker until explicit release.

## Independent accounting acceptance and review data checks

Evidence worker completed operation reporting, corrected CLI nested totals formatting, regenerated types/appendices, and reported 21 CLI tests, 4 HTTP tests, 3 contract/freshness tests and API quality passing. Primary reviewed root-only designer authorization, original-agent visibility, malformed correlation rejection and conservative zero-row coverage. Test stack returned to primary.

Primary combined command: `./test.sh tests/unit/jobs/platform/test_agent_evaluation.py tests/unit/jobs/platform/test_agent_evaluation_completion_race.py tests/unit/services/test_quality_usage_reporting.py tests/e2e/api/test_agent_evaluation_usage_reporting.py tests/unit/test_usage_report_cli.py tests/unit/test_agent_tests_recorded_cli.py tests/unit/test_contract_version.py tests/unit/test_skill_appendix_fresh.py tests/unit/test_agent_reviews_data_foundation.py -v`: 76 passed, 4 failed. Log `/tmp/workbench-accounting-and-review-data-primary.log`. All reporting/CLI/contract checks passed. Failures were traced to three review-data fixture FK insertion orders and a synthetic full-flow fixture missing an admitted baseline run/frozen case definitions; corrected without weakening checks.

Primary rechecked both affected modules with `./test.sh tests/unit/jobs/platform/test_agent_evaluation.py tests/unit/test_agent_reviews_data_foundation.py -v`: 39 passed, zero failures/errors/skips, confirmed in per-worktree JUnit. Log `/tmp/workbench-accounting-review-fixtures-primary.log`. Shared cleanup also verifies no scoped semantic jobs/ledger/attempt rows remain. The bounded accounting API/CLI slice is accepted; full backend/UI gates and production reporting UI are not complete. Primary API quality gate for the integrated source is running in `/tmp/workbench-accounting-review-quality-primary.log`.

Review data foundation includes definitions/versions, immutable review admission/evidence rows linked to canonical jobs, additive findings evidence/kind/provenance, and migration `20260920_agent_reviews`. Finding provenance uses immutable UUID snapshots instead of SET NULL FKs so deletion cannot erase the marker needed to deny inaccessible derived content. Agent/org deletion retains existing cascade behavior; accounting operation UUIDs survive domain deletion. Data tests passed; no review model executor/API/CLI is implemented by this slice. Evidence worker is drafting the next bounded service/executor handoff, read-only.

Integrated primary `./test.sh quality api` passed: 0 errors/warnings/informations and Ruff clean; `/tmp/workbench-accounting-review-quality-primary.log`. Review data foundation is accepted for the bounded storage slice. Restarted only this worktree's debug init to apply migrations (exit 0, migrations reported successful), then API as required for schema changes. No stack teardown or data reset. Public preview URL remains `https://bifrost-69c4f3b1-kkkh.eu1.netbird.services`. No review automation or production usage UI completion is claimed.

## On-demand review executor independently verified

Primary reviewed and corrected profile/config identity resolution, explicit bounded model response schema, actual recorded-reader root identity, frozen input size/fingerprint and job/requester bindings, exact source identities, and idempotent finding provenance. Actual usage settles independently before response parsing and result fencing. No paid calls were made by tests.

Worker reported 14 executor DB tests and 30 parser/reporting/contract/import/runtime checks passing after fixing fixture ownership and generic authorization error mapping. Its API quality check exposed a return-type error in the concurrently written finding visibility module; that correction remains owned by the visibility worker, with integrated quality still required.

Primary independently ran `./test.sh tests/e2e/api/test_agent_review_executor.py tests/unit/services/test_agent_reviews.py tests/unit/services/test_quality_usage_reporting.py tests/e2e/api/test_agent_delegation_lifecycle.py::test_child_terminal_before_parent_wait_commit_wakes_same_parent -v`: **38 passed**, zero errors/failures/skips in JUnit; log `/tmp/workbench-review-executor-primary.log`. This includes the summary-profile attribution correction and the early-child terminal-before-parent-wait regression. Phase 3B source and focused behavior accepted; integrated quality remains pending visibility changes.

Finding visibility is assigned under `2026-09-20-review-findings-visibility-handoff.md`. Primary review required SQL NULL-safe universal validation, actual shared run-policy reuse, base agent visibility for global count/page queries, and dependency-ordered fixture writes. Existing public findings routes must filter review-derived content before reads or mutation. No acceptance yet.

Next bounded packet: `2026-09-20-review-api-cli-handoff.md`, review definitions/admission/results/usage and top-level agent-reviews CLI. Scheduling proposal is documented but unapproved. Production UI, automation, proposed tools and requested integrated broad gates remain unfinished.

Primary independently ran `./test.sh tests/e2e/api/test_agent_finding_visibility.py tests/e2e/api/test_agent_findings.py -v`: **23 passed**, zero failures/errors/skips, log `/tmp/workbench-finding-visibility-primary.log`. Reviewed the corrected shared predicate: base agent policy, explicit SQL true checks, guarded JSON arrays, and direct canonical run-policy reuse before count/page and mutation. Existing manual finding shapes and inactive-agent behavior retained. Bounded visibility source/behavior accepted; integrated API quality still required after current review API/CLI implementation stabilizes.

Pinned PydanticAI 2.35.3 source confirms normalized inclusive input/cache token semantics. Recorded-token cache fraction is valid where usage coverage supports it; legacy missing capture remains unknown. Evidence doc and master UI acceptance updated. No product ratio or UI implementation claimed by this read-only audit.

## Review API/CLI corrections verified; Findings API/CLI implemented (2026-09-20)

Primary ran point directly in `durable-agent-platform-backend` (a dispatched cheaper session was parked with an explicit hold; OpenCode `ses_f407361e0ffeZ3c5QxsnLq2I87` left stopped). Test stack `bifrost-test-69c4f3b1` and debug stack `bifrost-debug-69c4f3b1` both UP; no stack teardown or data reset.

Review verification: `./test.sh tests/e2e/api/test_agent_reviews.py tests/unit/services/test_agent_reviews.py tests/unit/test_agent_reviews_cli.py tests/unit/test_contract_version.py` → 31 passed. DTO/skill/findings/visibility: 86 passed. `./test.sh quality api` → pyright 0 errors/warnings, Ruff clean. Generated `client/src/lib/v1.d.ts` was stale (`AgentReviewRunResults.findings` still `unknown[]`); regenerated from the worktree debug OpenAPI so findings are now `FindingPublic[]`. Skill appendices were fresh for the Review slice.

Findings slice implemented per `2026-09-20-findings-api-cli-handoff.md`: Finding DTOs moved unchanged into `api/shared/models.py` with additive `finding_kind`/`evidence_markdown`/read-only provenance plus `FindingSearchPage`; `FindingPublic` placed before `ReviewRunResults`; `src/models/contracts/agent_findings.py` is now an exact-name re-export shim. New `shared/agent_finding_queries.py` owns search/count/page and the single authorized serializer (linked cases, Markdown, provenance); the findings router and `shared/agent_review_admission.py:get_review_results` both use it. Router keeps the bare-list shape/order and adds literal `GET /api/agent-findings/search` (before `/{finding_id}`) with status/kind/source/review/review-run/query/admin-org filters, limit 50 default/max 200, `created_at` desc + id order, literal escaped case-insensitive `q` (max 200), nonadmin org-expansion → 403. Explicit-null evidence clears on update via `model_fields_set`; provenance fields stay `extra=forbid`. New `agent-findings` CLI group (create/search/list/get/update, dismiss via update, `--clear-evidence-markdown`, `--json` scriptable, local + server-422 errors exit 2, no provenance flags) registered in `ENTITY_GROUPS`. Contract fingerprint refreshed as additive (`d8f0cf70...`); no `MIN_CLI_VERSION` change. Client `v1.d.ts`, `cli-reference.md`, `openapi-digest.md` regenerated. Five existing client consumers got the mechanical `finding_kind: "problem"` default (no UI redesign).

Focused verification: 87 passed (`test_agent_findings`, `test_agent_findings_cli` new 8, `test_agent_reviews_cli`, `services/test_agent_reviews`, `test_agent_findings`, new `test_agent_findings_search` 6, `test_agent_finding_visibility`, `test_agent_reviews`, `test_agent_review_executor`); contract/DTO/freshness 65 passed; `./test.sh quality api` green; client `tsc` clean; `lint` 0 errors (1 pre-existing e2e-support warning); vitest 27 passed (FindingsPanel, SuiteWorkspace, agentPlatform). Broader suites not run: full backend `./test.sh all`, full Vitest, Playwright, `./test.sh pre-pr`. No commit/push/merge.

Remaining (not authorized in this slice): scheduling, Phase 4 test-collection/proposed-tool contracts, UI redesign/implementation, tuning-path retirement. Final code review with Astra and product-intent review still required. Feature not declared complete.

## Codex Sol review (2026-09-20, `gpt-5.6-sol`, `codex review -`)

Independent read-only review of the Findings slice, scoped by file list. Invariants 1–7 confirmed clean (visibility before count/page, serializer sharing, provenance immutability, dismissal independence, CLI exit codes). It also re-ran the 6 search E2E tests itself: passed. One P2 accepted and fixed: `FindingCreate`/`FindingUpdate`/`FindingPublic`/`FindingSearchPage` are CLI-consumed but were missing from `_COMMAND_DTOS` in `test_contract_version.py`, so a future retype would not flip the tripwire. Added all four; fingerprint refreshed as additive (`672a1d3d…`), no `MIN_CLI_VERSION` change. Contract/DTO/freshness/CLI re-verified: 85 passed. Parked cheaper session `ses_f3efb39d` remains idle with zero edits; no terminate action exists in the session tool, so hard-removal is left to the OpenChamber UI.

## Scheduling S1 implemented and review-looped (2026-09-21)

Primary approved `2026-09-20-quality-scheduling-approved-handoff.md`, resolving the proposal's open gaps (shared trigger/fire tables, no retention deleter, internal-only receipts, skip-only overlap). S1: `recurring_platform_job_triggers` + `recurring_trigger_fires` ORM with skip-only CHECK and `(trigger_id, scheduled_for)` fence, additive migration `20260920_recurring_triggers`, `shared/recurring_trigger_registry.py` validation, `shared/review_schedule_admission.py` (window selection over every TERMINAL_STATUSES state, requester validated at fire time, schedule-aware dedupe, same evidence/profile/fingerprint/enqueue helpers as HTTP), `process_recurring_platform_job_triggers` leader callback registered every minute plus diagnostics entry. No API/CLI/UI/routes; evaluation-suite definition deferred to S2.

Codex review round 1 returned 2 P1 + 2 P2. Accepted three: system-account requesters now rejected (superusers follow HTTP `authorize_review_agent` semantics; packet amended; new tests), selection widened from `completed`-only to all terminal statuses (evidence loader already accepts them; new test), overlap CHECK tightened to skip-only in ORM+migration (new constraint test). Disagreed on one with evidence: test-queued-job race is shared identically with long-accepted on-demand admission tests, worker logs show zero agent.review claims, cleanup cancels-then-deletes. Round 2 found one real P2 (late readability failure left orphan job+run behind a skipped receipt); fixed by moving the guard before any writes, plus an orphan regression test. Final: 31 scheduling tests + 165-test regression (scheduling, reviews, findings, executor, scheduler, contract/DTO/freshness) green; `./test.sh quality api` clean. Migration applied via the hash-gated template rebuild. No commit/push/merge. Next: S2 evaluation-suite admission extraction, then schedule API/CLI, then Phase 4/UI lanes.

## S2 evaluation-suite scheduling looped (2026-09-21)

Extracted single/batch matrix orchestration from `agent_evaluations.py` into `shared/evaluation_matrix_admission.py` (domain errors, same locks/quotas/dedupe/membership; router endpoints are thin translators). Registered `agent_evaluation_suite` trigger params, added `suite_schedule_admission.py` (savepoint-wrapped batch call, matrix-id fire receipt with first-cell job witness, member-execution overlap), wired the processor branch. Behavior parity proven by the untouched matrix/batch/API/authorization suites. Codex round 1: P1 partial-commit on mid-batch failure (fixed via savepoint + rollback test), P2 test cancel-first (paired/duplicate reworked to cancel immediately; overlap keeps one live job under the documented shared exposure), P3 packet contradiction (handoff now documents S2). Round-2 fixes verified: 429-test gate (scheduling, matrix, evaluation API/auth/services, scheduler, reviews, findings, executor, contract/DTO/freshness) green, quality clean. Incidents: template rebuild for the tightened migration, fire/job delete ordering vs the admitted CHECK, expired-ORM-object access in test finally blocks (pre-captured ids). No commit/push/merge.
Confirmation review accepted the savepoint/test/doc fixes except one P2 (rollback test asserted matrices+executions but not jobs); added the job assertion, green. S2 closed: 429-test gate + quality clean, no commit/push/merge.

## S3 schedule API/CLI looped (2026-09-21)

Trigger CRUD (`POST/GET` collection, `GET/PATCH` item, read-only per-trigger fires; no delete) plus `recurring-triggers` CLI (create/list/get/update/disable/fires, exit-2 usage errors). Org derived from target, params must reference operation_id, registry validation on every write, requester always the caller. Fingerprint additive refresh; types + appendices regenerated. Codex round 1: P1 requester-rebind on update (fixed — any update rebinds to its editor — plus test); P2 Click-parse errors not JSON (disagreed and deferred: established framework-wide pattern shared with the accepted reviews/findings CLIs; fix belongs in a dedicated CLI-error packet, recorded as follow-up). Final: 28 focused tests + quality clean, tsc clean. Follow-ups banked: CLI JSON-parse-error parity (framework-wide), parked-session UI removal, Astra code/design review, full gates, commit/PR decision.

## Phase 4a identity + default-collection looped (2026-09-21)

Decisions: Option A logical_test_id, hidden per-(org,agent) default suites (partial-unique + scope CHECK; existing name key scoped to non-defaults), auto-published append-only defaults, copy-to-default edits, deferred apply endpoint. Slice: migration 20260921, ORM, get_or_create_default_suite (savepoint race collapse), agent-delete removes defaults (named keep SET NULL). Codex round 1 (3 P1, all fixed): old name-key collision (partial index), savepoint-before-insert + true conflict-path test, agent-delete CHECK breakage (delete defaults in delete_agent). Confirmation round found downgrade-duplicate + untested-conflict-path: downgrade now renames defaults apart (proven on scratch DB with duplicate defaults: downgrade renames + restores legacy key, upgrade-back clean, scratch dropped), conflict path covered by blinded-lookup test. Final: 6 collection tests + 259-test gate (management, matrix, evaluation API/auth/services) + quality clean. Incidents: partial unique must be an index not a constraint, migration op ordering (column before partial index), test identity-map staleness (populate_existing reads), template-rebuild pool race (stop consumers first), `//` comment-typo habit (caught twice, going forward: Python comments use `#`). No commit/push/merge.
 Round 3 (agent-delete lifecycle across all paths): accepted — route-local cleanup replaced with a DB trigger (trg_agents_delete_default_suites) covering route, sync sweep, indexers, and future writers; named suites keep SET NULL. Proven by a raw-SQL delete test plus the route test. Downgrade/upgrade round-trip proven on scratch DB. No commit/push/merge.
 4b fixes round 2: current version re-read inside the suite lock (concurrent stale edits now observe the winner); omission test proves non-null preservation separately from null-clearing.
 4c fixes: recorded readability per evaluation (ownership + live source runs, omit on failure), roles eager-load on all agent loaders (4b loader had the same bug), terminal result statuses via canonical set. Tests: revocation omission, running-row exclusion, role_based 404-not-500. Confirmation pending.
 4d fixes: reuse-before-quota (identical requests reuse at quota), disabled selections rejected, subset DTOs fingerprinted, strict CLI tokens. Confirmation pending.
 4d closed clean on confirmation.
 4e fixes: behavior rule field types mirrored, raw-result output validation (scalar/array contracts), malformed declarations fail closed. Confirmation pending.
 4e closed: confirmation found one more malformed-declaration gap (non-list proposed_tools ignored); fixed + covered. 24 unit tests + quality clean.
 4f fixes: findings via canonical visibility predicate; all runs authorized before history cap (cap applies to history only); tests for broken provenance + beyond-cap revocation. Confirmation pending.

## Loop closure: backend slices S1–S3 + 4a–4f complete (2026-09-21)

Every slice implemented → focused tests → API quality → Codex review → findings resolved → confirmation review clean. Consolidated final gate: 140 + 402 backend tests green across scheduling, reviews, findings, evaluation, collection, CRUD/latest/run/generate, management, contracts and freshness; `./test.sh quality api` clean; client `tsc` clean; `lint` 0 errors (1 pre-existing warning). Generated types/appendices and contract fingerprints current. No commit/push/merge. Remaining lanes need other owners: production workbench UI + final code/design review (Astra), tuning-path retirement (product review), commit/PR decision (user), full gates (merge queue). Banked follow-ups: CLI JSON-parse-error parity (framework-wide), parked-session UI removal.
