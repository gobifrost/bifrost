# Approved packet A2: authorized recorded evidence projection

Execute only after primary accepts A1. Executor Muse Spark 1.3 Contributor; primary retains review. This packet is a real backend capability, not prototype work.

Allowed new files: `api/shared/agent_recorded_evidence.py`, `api/tests/unit/services/agent_evaluations/test_recorded_evidence.py`, `api/tests/e2e/api/test_agent_recorded_evidence.py`, `docs/superpowers/plans/2026-09-20-recorded-evidence-report.md`. Do not edit runtime/legacy/synthetic paths or A1. No migrations/routes yet. No commits/push/install/secret access. New business logic belongs in shared. Preserve other dirty work.

Provide `load_recorded_run_evidence(session, run_id, *, user)` (mandatory UserPrincipal, not an unchecked UUID-only loader) -> `{evidence, completeness, evidence_refs, limitations}` ready for A1. Run visibility via canonical `agent_run_visibility_conditions` plus explicit tenant comparison for non-superusers (including org=None). Root selection never expands to ancestor or siblings. Start at selected run; descendants included only through parent linkage, with visibility and same tenant enforced per node. Do not leak hidden IDs/counts/content via limitations: generic incomplete tree reason is enough. Non-visible and nonexistent top-level IDs yield same domain not-found error. No writes, external calls or synthetic-session lookup. Bound tree/journal/invocation/usage rows before processing; overflow raises explicit domain oversized error, never truncate and mark complete.

Evidence sources: durable run fields, journal, invocation ledger, usage seq>0, and legacy steps only as explicitly incomplete observational evidence. Redact prior to returning. Source references use run_id+sequence+kind; operation identity can accompany them. Dedupe the same call in journal and invocation, but don't collapse distinct repeated calls by tool name. Pending/planned operations must not be counted as successfully executed side effects. Uncertain invocation is an observed call but incomplete result, never replayed.

Establish completeness from the actual runtime recording contract, not merely nonempty arrays. Require durable terminal evidence, unbroken journal sequences and consistent invocation/journal correspondence to prove a complete call history. Legacy-only/partial/missing-terminal traces remain incomplete. A missing ledger row or unmatched call must not permit a no-call pass. Presence of a checkpoint alone is not completeness. Document exact proof and relevant source symbols. If the runtime cannot substantiate a dimension, return false with limitation instead of inventing proof.

Single run ordering from journal sequence can be complete; multiple parallel children do not acquire a global causal order from timestamps, so tool_order false for multi-run projection absent explicit established causality. Ensure argument redaction marks tool_arguments false if values needed to evaluate were lost. Missing simulator_state stays absent. Real execution count only if dispatch evidence can establish it; do not infer all non-engine tool_call journal entries executed (schema rejection can occur before dispatch). If real-tool completeness cannot be proven, omit or mark false.

Output explicitly recorded null differs from absent evidence: a selected terminal run's output key may be None and is evaluated as null when proven recorded; legacy missing output provenance should remain incomplete. Usage counters exclude sequence0 summarization. Zero usage is not assumed from missing rows. Complete finite scalar counters only when recording semantics prove them; do not sum root aggregate + child aggregates twice. A1 completeness keys must match exactly. All evidence is immutable snapshot data to be frozen by later job admission; this service itself does not persist it.

Tests: live DB integration fixtures for cross-tenant denial/private chat delegation denial, explicit org None isolation, selected child not expanding to parent/sibling, hidden child withholding content and marking tree incomplete, full terminal single-run trace and empty complete history, incomplete/legacy/uncertain trace, duplicate call journal+ledger, two same-name distinct calls, parallel order incomplete, redaction, seq0 usage exclusion, no real calls/writes, overflow, deleted/missing. Keep minimal deterministic fixtures and cleanup. One e2e file can exercise auth plus durable-row projection without adding an HTTP route.

Run `./test.sh tests/unit/services/agent_evaluations/test_recorded_evidence.py tests/e2e/api/test_agent_recorded_evidence.py -v`, A1 tests and `./test.sh quality api`. Record precise known failures; do not rerun blindly. Report core completeness proof and any conservative unsupported dimensions. Return to primary; no user notifications.
